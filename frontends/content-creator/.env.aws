# AWS 환경 - Nginx 리버스 프록시를 통한 상대경로
# Nginx proxy_pass가 /api/를 붙여주므로 프론트에서는 /api/ 불필요
# 예: /api/progression/something → Nginx → backend:8084/api/something
#
# content-creator는 로컬에서 baseURL에 /api 포함 (http://localhost:8089/api)
# → AWS에서는 Nginx가 /api/를 붙여주므로 /api/progression만으로 충분

# Backend API Endpoints
VITE_API_BASE_URL=/api/progression
VITE_PROGRESSION_API_URL=/api/progression
VITE_PAYMENT_API_URL=/api/payment
VITE_DELIVERY_API_URL=/api/ecommerce

# Keycloak Configuration
VITE_KEYCLOAK_URL=/keycloak
VITE_KEYCLOAK_REALM=hamster-world
VITE_KEYCLOAK_CLIENT_ID=content-creator
