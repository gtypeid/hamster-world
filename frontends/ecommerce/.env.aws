# AWS 환경 - Nginx 리버스 프록시를 통한 상대경로
# VITE_API_BASE_URL: lib/api.ts (fetch 기반)에서 사용
# VITE_ECOMMERCE_API_URL: api/client.ts (axios 기반)에서 사용
# 두 API 클라이언트가 각각 다른 변수를 참조하므로 둘 다 설정 필요
VITE_API_BASE_URL=/api/ecommerce
VITE_ECOMMERCE_API_URL=/api/ecommerce
VITE_KEYCLOAK_URL=/keycloak
VITE_KEYCLOAK_REALM=hamster-world
VITE_KEYCLOAK_CLIENT_ID=ecommerce
