# 🐹 Hamster PG Admin

햄스터 PG 서비스의 관리자 페이지입니다.

## 🎯 주요 기능

- 🌰 **MID 관리**: 가맹점 정보 생성 및 조회
- 🎡 **거래 내역**: 결제 거래 내역 실시간 모니터링

## 🛠️ 기술 스택

- **React 19** + **TypeScript**
- **Vite** - 빌드 도구
- **React Router v7** - 라우팅
- **TanStack Query v5** - 서버 상태 관리
- **Zustand** - 클라이언트 상태 관리
- **Tailwind CSS** - 스타일링
- **Axios** - HTTP 클라이언트

## 📋 사전 요구사항

- Node.js 18 이상
- npm 또는 pnpm
- hamster-pg-service가 실행 중이어야 합니다 (포트 8083)

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

브라우저가 자동으로 열리며 `http://localhost:3000`에서 확인할 수 있습니다.

### 3. 빌드

```bash
npm run build
```

빌드 결과물은 `dist/` 폴더에 생성됩니다.

### 4. 프로덕션 미리보기

```bash
npm run preview
```

## 🌐 환경 변수

```.env
VITE_API_BASE_URL=http://localhost:8083
```

- `.env.development` - 개발 환경
- `.env.production` - 프로덕션 환경

## 📁 프로젝트 구조

```
frontends/hamster-pg/
├── src/
│   ├── api/                    # API 클라이언트
│   │   ├── client.ts           # Axios 인스턴스
│   │   ├── midApi.ts           # MID API
│   │   └── paymentApi.ts       # Payment API
│   │
│   ├── features/               # 기능별 모듈
│   │   ├── mid/                # MID 관리
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   └── types.ts
│   │   └── payment/            # 거래 내역
│   │       ├── components/
│   │       ├── hooks/
│   │       └── types.ts
│   │
│   ├── components/             # 공통 컴포넌트
│   │   ├── layout/
│   │   │   ├── AppLayout.tsx
│   │   │   ├── Header.tsx
│   │   │   └── Sidebar.tsx
│   │   └── ui/
│   │       ├── Button.tsx
│   │       ├── Badge.tsx
│   │       ├── LoadingSpinner.tsx
│   │       └── EmptyState.tsx
│   │
│   ├── types/                  # 공통 타입
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
│
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

## 🎨 햄스터 테마

이 관리자 페이지는 귀여운 햄스터 테마를 사용합니다! 🐹

- **햄스터 오렌지** (#F59E0B) - 주요 색상
- **햄스터 브라운** (#92400E) - 텍스트
- **햄스터 베이지** (#FEF3C7) - 악센트
- **햄스터 아이보리** (#FFFBEB) - 배경

## 🔥 주요 특징

### 1. 스마트 컴포넌트 패턴
- Custom Hook으로 로직 분리
- 컴포넌트가 직접 TanStack Query 훅 호출
- 간결하고 읽기 쉬운 코드

### 2. 실시간 업데이트
- 거래 내역은 5초마다 자동 리페치
- TanStack Query의 캐싱으로 빠른 응답

### 3. 사용자 친화적 UI
- 로딩 상태: 햄스터가 일하는 애니메이션
- Empty State: 귀여운 메시지
- 모달 상세 보기

## 🛣️ 라우트

- `/` - 자동으로 `/mids`로 리다이렉트
- `/mids` - MID 관리 페이지
- `/payments` - 거래 내역 페이지

## 📝 개발 가이드

### MID 생성

1. "🐹 MID 생성" 버튼 클릭
2. 가맹점명 입력
3. 생성 완료 시 API Key와 Secret Key 표시
4. ⚠️ Secret Key는 다시 확인 불가하니 안전하게 보관

### 거래 내역 확인

1. 거래 내역 페이지 접속
2. 테이블에서 원하는 거래 클릭
3. 모달에서 상세 정보 확인

## 🐛 트러블슈팅

### API 연결 오류

```
😵 데이터를 불러오는데 실패했어요
```

**해결 방법:**
1. hamster-pg-service가 실행 중인지 확인 (포트 8083)
2. `.env.development`의 `VITE_API_BASE_URL` 확인
3. CORS 설정 확인

### 빌드 오류

```bash
# node_modules 삭제 후 재설치
rm -rf node_modules package-lock.json
npm install
```

## 📦 배포

### Nginx 설정 예시

```nginx
server {
    listen 80;
    server_name hamster-pg-admin.example.com;

    root /var/www/hamster-pg-admin/dist;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:8083;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🤝 기여

버그 리포트나 기능 제안은 이슈로 등록해주세요!

## 📄 라이선스

MIT

---

Made with 🐹 by Hamster Team
