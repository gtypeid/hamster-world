package com.hamsterworld.progression.domain.seasonpromotion.constant

/**
 * Promotion Target Role (프로모션 대상 역할)
 */
enum class PromotionTargetRole {
    CUSTOMER,   // 구매자 (VIP 멤버십)
    RIDER       // 라이더 (프리미엄 구독)
}
